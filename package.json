{
  "name": "qubartech-erp-root",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix web dev",
    "build": "npm run --prefix web build",
    "start": "npm run --prefix web start",
    "prisma:generate": "npm run --prefix web prisma:generate",
    "prisma:migrate": "npm run --prefix web prisma:migrate",
    "prisma:deploy": "npm run --prefix web prisma:deploy",
    "seed": "npm run --prefix web seed"
  },
  "dependencies": {
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  }
}
